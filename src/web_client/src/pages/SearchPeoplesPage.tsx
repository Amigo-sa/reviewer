import * as React from 'react';

class SearchPeoplesPage extends React.Component {
    public render() {
        return (
            "Search peoples page"
        );
    }
}

export default SearchPeoplesPage;